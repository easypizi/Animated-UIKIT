import type { Meta } from '@storybook/react';
import { Popper } from '..';

export default {
  component: Popper,
  title: 'UI/Utils/Popper',
} as Meta<typeof Popper>;

export { Playground } from './Playground.stories';
