bootstrap:
	npm install

clean:
	npm run clean

build:
	npm run build

lint:
	npm run lint

storybook:
	npm run storybook:start

icons:
	npm run generate-icons

version: 
	npm run version-update
	
deploy: 
	npm run deploy
